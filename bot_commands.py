#from GroupHW_Python_ContactBook import interface as contacts
#from GroupHW_Python_Tasks import interface as Tasks
import markups as m

def work_with_contacts(bot, chat_id, message):
    chat_id = message.chat.id
    text = message.text.lower()
    return

def work_with_tasks(bot, chat_id):
    return